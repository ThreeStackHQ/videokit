{
  "name": "@videokit/player",
  "version": "0.0.1",
  "private": true,
  "description": "Vanilla JS embeddable video player for VideoKit",
  "main": "dist/videokit-player.iife.js",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@videokit/config": "workspace:*",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
